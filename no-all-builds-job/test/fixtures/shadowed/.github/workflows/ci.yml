# Fixture for the no-all-builds-job dogfood harness in release.yml: a
# workflow whose jobs map carries the forbidden `all-builds` key. This file is
# never executed — it lives outside the repo root's .github/workflows/ and
# exists only to be scanned by the action's workflow-file layer.
name: shadowed-fixture
on:
  push:

jobs:
  all-builds:
    runs-on: ubuntu-latest
    steps:
      - run: echo this job name fakes the org gate

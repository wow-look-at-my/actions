name: Test yaml-comment-block
version: 1
description: Dogfood the comment-block check against this repo and both fixtures (never released)

runs:
  using: composite
  steps:
    - name: Check passes on this repo
      uses: ./yaml-comment-block

    # see docs/release-workflow.md
    - name: Check must fail on the wall fixture
      id: wall
      continue-on-error: true
      shell: bash
      run: GITHUB_WORKSPACE="${GITHUB_WORKSPACE}/yaml-comment-block/test/fixtures/wall" node yaml-comment-block/dist/index.js

    - name: Assert the wall fixture failed the check
      shell: bash
      run: test "${{ steps.wall.outcome }}" = "failure"

    - name: Check must pass on the at-the-limit fixture
      shell: bash
      run: GITHUB_WORKSPACE="${GITHUB_WORKSPACE}/yaml-comment-block/test/fixtures/clean" node yaml-comment-block/dist/index.js

name: Fixture at the limit

# One comment line in a row is the whole allowance.
on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    # A second block starts after a line of content.
    steps:
      - run: echo hi # trailing comments never join a block

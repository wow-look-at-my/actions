name: Fixture with a comment wall

# This fixture exists to prove the check fails.

# A blank line does not split the block, so this is the second line.
on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo hi

name: Smart Cache
version: 1
description: Cache with change detection - only saves when files actually changed

inputs:
  path:
    description: 'Paths to cache (space-separated)'
    required: true
  key:
    description: 'Cache key'
    required: true

outputs:
  cache-hit:
    description: 'Whether cache was restored'

runs:
  using: node20
  main: dist/index.js
  post: dist/index.js
  post-if: always()

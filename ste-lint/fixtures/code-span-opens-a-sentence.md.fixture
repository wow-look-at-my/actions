<!-- expect: hardLong=0 -->
Its order is deny, hooks, allow, mode, then prompt.
`02-contracts.md` fixes it there, so those later milestones have a socket to
plug into rather than a negotiation to have.

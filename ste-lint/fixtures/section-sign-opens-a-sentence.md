<!-- expect: hardLong=0 -->
The reply goes the long way round via `app.Answer`.
§3a now states explicitly that a prompt does not require a run, so an install
consent from a slash command works the way a permission prompt does.
